import React from "react";

const VideoCallBadge = () => (
  <span className="inline-flex items-center gap-1 px-2 py-1 bg-green-100 text-green-700 text-xs font-semibold rounded-full animate-pulse">
    🟢 Call Available
  </span>
);

export default VideoCallBadge;